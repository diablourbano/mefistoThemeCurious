<header class="post-header {{#if feature_image}}has-image{{else}}no-image{{/if}}">
    <div class="post-header-inside{{#if feature_image}} bg-dark bg-full{{/if}}">
        {{#if feature_image}}
            <img
                class="bg-full-image bg-fade-in"
                srcset="{{img_url feature_image size="xs"}} 300w, {{img_url feature_image size="s" }} 600w, {{img_url feature_image size="m" }} 800w, {{img_url feature_image size="l" }} 1200w, {{img_url feature_image size="xl" }} 1600w, {{img_url feature_image size="xxl" }} 2000w"
                sizes="(min-width: 1600px) 1600px, 100vw"
                src="{{img_url feature_image size="xl"}}"
                alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
            />
        {{/if}}
        <div class="post-header-content inner">
            {{#if tags}}
                <div class="post-tags">
                    {{#foreach tags}}
                        <a href="{{url}}" class="button button-small button-solid-alt">{{name}}</a>
                    {{/foreach}}
                </div>
            {{/if}}
            <h1 class="post-title">{{title}}</h1>
            <div class="post-meta">
                {{t "By"}} {{authors}} {{t "on"}} <time datetime="{{date format='YYYY-MM-DD'}}">{{date format="LL"}}</time>
            </div>
            <div class="post-reading-time">{{reading_time minute=(t "1 min read") minutes=(t "% min read")}}</div>
        </div>
    </div>
    {{#if feature_image}}
        {{#if feature_image_caption}}
            <div class="post-header-caption inner">
                {{feature_image_caption}}
            </div>
        {{/if}}
    {{/if}}
</header>
