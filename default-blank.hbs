<!DOCTYPE html>
<html lang="{{@site.locale}}"{{#match @custom.color_scheme "Dark"}} class="dark-mode"{{/match}}>
    <head>
        {{!-- Document Settings --}}
        <meta charset="utf-8">
        {{!-- Page Meta --}}
        <title>{{{block "blank-meta-title"}}} - {{@site.title}}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        {{!-- Styles --}}
        {{> custom-fonts}}
        <link rel="stylesheet" type="text/css" href="{{asset "css/style.min.css"}}">
        {{{block "blank-background-image-style"}}}
        {{!-- Outputs SEO meta+structured data and other important settings --}}
        {{ghost_head}}
    </head>
    <body class="blank-template">

        {{!-- Inserts the main content --}}
        {{{body}}}

        {{!-- Outputs important scripts and data --}}
        {{ghost_foot}}

    </body>
</html>
